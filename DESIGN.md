# citroam design direction

详细体验见 [产品设计](./specs/product-design.md)，范围与验收见 [产品需求](./specs/product-requirements.md)。

## Product idea

citroam 是一块简单、易用、随性、自由的生活待办画布。

> 想到什么就丢进画布，怎么整理由你决定。

产品链路只有：

```text
丢进画布 → 随手维护 → 完成
```

## Product model

- `Canvas`：一个本地工作空间；当前视图是随手页或某个即时派生的日期页。
- `Card`：一句话、一个想法或一项待办，不区分类型。
- `日期页`：按本地日期即时派生的画布页面，像翻书一样前后浏览，不需要创建日期卡片。
- `总览`：同一批 Card 的规整列表投影，用来查看全部内容和定位回画布。
- `Area`：用户按需画出的命名区域，不附带项目规则。

## Interaction rules

- 输入一句话，按 Enter 创建。
- 拖动就是移动，勾选就是完成。
- 日期、备注和优先级全部可选。
- 不整理也能持续记录、搜索和完成。
- 画布不自动移动、分类、排序或隐藏开放 Card。
- 日期页只提供随时、上午、下午、晚上四个柔性围栏；Card 可以留在围栏之外。
- 拖到围栏时先预览，释放时才吸附并建立时间关联；拖出围栏则回到“随时”。
- 空白日期页不显示额外解释文案，底部捕获栏就是入口。
- 搜索、查看全部、撤销和本地保存负责兜底，但不增加主链路。

## Visual read

方向命名为 `Juicy Utility`。它把现代果汁与汽水包装的清爽色彩、柔软形状和轻快反馈转译成每天可用的桌面工具。

> 80% 是清晰可靠的工具，20% 是饮料品牌式的轻松与玩心。

- `DESIGN_VARIANCE: 8`
- `MOTION_INTENSITY: 6`
- `VISUAL_DENSITY: 4`

- 淡青柠 Canvas、苏打白 Surface 和深橄榄 Ink 构成长期使用的底色。
- 焦橙是唯一强强调色；柠檬、青柠和薄荷只作为稳定的浅色 Surface，不随机赋予 Card。
- Card 像一张柔软的饮料标签，使用不对称软圆角、轻微实体偏移阴影和气泡式完成控件。
- Area 像半透明的青柠托盘，仍然只表达用户自己理解的空间范围。
- 当前“三个气泡加一个 Plus”的标记是品牌记忆符号，延伸到加载、空状态和完成反馈。
- 动效是产品性格的一部分。创建有 pop，拿起有 lift，放下有轻微 squash，完成像气泡轻轻啵掉。
- 拖动和平移必须完全跟手，不能为了弹性效果加入输入延迟。
- 动画只用于确认操作、解释层级和连接状态。除加载反馈外，不使用常驻循环。
- 鼠标与键盘是桌面发布基线，完整支持 `prefers-reduced-motion`；触控只有在单指平移、Card 拖动与双指缩放全部通过真机验收后才对外宣称可用。
- 不使用随机角度、随机颜色、持续漂浮、庆祝粒子或满屏水果装饰。

视觉目标是像一罐刚打开的柑橘汽水：清爽、有气泡感、容易亲近，同时仍然是一件可以长期使用的可靠工具。

## Product boundaries

不建设主动规划型 AI、Now、情境匹配、自动重新浮现、卡片类型、连线、子画布、复杂项目层级、团队协作或完整白板。原生对话 Agent 是画布上的一等操作层：它用一句话调用既有 Card 动作，结果可见、可预览或可撤销，失败时不改变画布，也不替用户规划、提醒、推荐或评价。生产路径通过 Tauri 按需启动真实 DeepSeek Harness sidecar，Harness 只暴露 `citroam_apply`；模型不能直接写 Workspace、文件或坐标。简单不是暂时缺功能，而是 citroam 的长期产品选择。

Agent 的舒适度定义为低打扰、少确认、结果可见、随时可撤销：

```text
进入对话 → 说一句 → 看见真实结果 → 继续生活
```

它只把自然语言翻译成既有 Card / Placement / Workspace action，由本地规则校验、原子提交并生成 Undo；模型不能直接写数据或生成坐标。普通捕获永远绕过 Agent；没有用户请求时 Agent 不主动发言或执行，断网、失败或上下文失效时画布保持原样，核心功能继续离线可用。

Agent 没有独立页面或需要配置的开关。标题栏的画布工具层始终提供“对话”入口，打开后只是覆盖当前画布的轻量面板；应用启动时不启动 Harness、不初始化模型、不发起网络请求；普通捕获、翻页、拖动、搜索和总览不经过 Agent，用户打开面板并发送明确请求后才按需启动或复用 sidecar。
